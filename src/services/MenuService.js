// const helper = require('../helpers/helperFunctions');

module.exports = {
    
    validaDados: (dados) => {
        let retorno = { status: true, msg: '' };

        // const { nome } = dados;

        // if (!nome) {
            // return retorno = {
                // status: false,
                // message: 'Nome é um campo obrigatórios'
            // }
        // }


        return retorno;
    }

}